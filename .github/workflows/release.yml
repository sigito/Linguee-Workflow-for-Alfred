name: Build release

on:
  push:
    branches: [ main ]
    paths:
      - LATEST_VERSION
      
  workflow_dispatch:

jobs:
  build:

    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2
      
    - name: Create release archive
      run: make
